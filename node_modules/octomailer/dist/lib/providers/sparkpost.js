"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SparkPostProvider = void 0;
// IMPORTANT: This provider is intentionally implemented using axios to avoid
// the official sparkpost package, which is deprecated and has unresolved security
// vulnerabilities. Do not revert to using the SDK.
const axios_1 = __importDefault(require("axios"));
const errors_1 = require("../errors");
class SparkPostProvider {
    constructor(apiKey, weight) {
        this.name = 'sparkpost';
        this.baseUrl = 'https://api.sparkpost.com/api/v1';
        this.apiKey = apiKey;
        this.weight = weight;
    }
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const payload = {
                content: {
                    from: options.from,
                    subject: options.subject,
                    html: options.html,
                    text: options.text,
                    attachments: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((a) => ({
                        name: a.filename,
                        type: a.contentType,
                        data: a.content,
                    })),
                },
                recipients: [{ address: options.to }],
            };
            try {
                const { data } = yield axios_1.default.post(`${this.baseUrl}/transmissions`, payload, {
                    headers: {
                        Authorization: this.apiKey,
                        'Content-Type': 'application/json',
                    },
                });
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`SparkPost Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.SparkPostProvider = SparkPostProvider;
