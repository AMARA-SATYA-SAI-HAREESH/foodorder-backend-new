"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SESProvider = void 0;
const client_ses_1 = require("@aws-sdk/client-ses");
const errors_1 = require("../errors");
const nodemailer_1 = __importDefault(require("nodemailer"));
/**
 * @class SESProvider
 * @description The Amazon SES provider for sending emails.
 */
class SESProvider {
    constructor(region, accessKeyId, secretAccessKey, weight) {
        this.name = 'ses';
        this.client = new client_ses_1.SESClient({
            region,
            credentials: {
                accessKeyId,
                secretAccessKey,
            },
        });
        this.weight = weight;
    }
    /**
     * @method send
     * @description Sends an email using Amazon SES.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const mail = nodemailer_1.default.createTransport({
                SES: { ses: this.client, aws: { SendRawEmailCommand: client_ses_1.SendRawEmailCommand } },
            });
            const mailOptions = {
                from: options.from,
                to: options.to,
                subject: options.subject,
                text: options.text,
                html: options.html,
                attachments: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((a) => ({
                    filename: a.filename,
                    content: a.content,
                    contentType: a.contentType,
                })),
            };
            try {
                const data = yield mail.sendMail(mailOptions);
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`SES Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.SESProvider = SESProvider;
