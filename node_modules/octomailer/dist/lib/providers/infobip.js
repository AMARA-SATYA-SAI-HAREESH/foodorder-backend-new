"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.InfobipProvider = void 0;
// IMPORTANT: This provider is intentionally implemented using axios to avoid
// the official @infobip-api/sdk package, which is outdated. Do not revert to using the SDK.
const axios_1 = __importDefault(require("axios"));
const form_data_1 = __importDefault(require("form-data"));
const errors_1 = require("../errors");
/**
 * @class InfobipProvider
 * @description The Infobip provider for sending emails.
 */
class InfobipProvider {
    constructor(apiKey, baseUrl, weight) {
        this.name = 'infobip';
        this.apiKey = apiKey;
        this.baseUrl = baseUrl;
        this.weight = weight;
    }
    /**
     * @method send
     * @description Sends an email using Infobip.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            const form = new form_data_1.default();
            form.append('to', options.to);
            form.append('from', options.from);
            form.append('subject', options.subject);
            if (options.text) {
                form.append('text', options.text);
            }
            if (options.html) {
                form.append('html', options.html);
            }
            if (options.attachments) {
                for (const attachment of options.attachments) {
                    form.append('attachment', attachment.content, {
                        filename: attachment.filename,
                        contentType: attachment.contentType,
                    });
                }
            }
            try {
                const { data } = yield axios_1.default.post(`${this.baseUrl}/email/3/send`, form, {
                    headers: Object.assign(Object.assign({}, form.getHeaders()), { Authorization: `App ${this.apiKey}` }),
                });
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`Infobip Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.InfobipProvider = InfobipProvider;
