"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PostmarkProvider = void 0;
const postmark_1 = require("postmark");
const errors_1 = require("../errors");
/**
 * @class PostmarkProvider
 * @description The Postmark provider for sending emails.
 */
class PostmarkProvider {
    constructor(serverToken, weight) {
        this.name = 'postmark';
        this.client = new postmark_1.ServerClient(serverToken);
        this.weight = weight;
    }
    /**
     * @method send
     * @description Sends an email using Postmark.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const message = {
                To: options.to,
                From: options.from,
                Subject: options.subject,
                TextBody: options.text,
                HtmlBody: options.html,
                Attachments: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((a) => ({
                    Name: a.filename,
                    Content: a.content,
                    ContentType: a.contentType,
                    ContentID: null,
                })),
            };
            try {
                const data = yield this.client.sendEmail(message);
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`Postmark Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.PostmarkProvider = PostmarkProvider;
