"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SendGridProvider = void 0;
const mail_1 = __importDefault(require("@sendgrid/mail"));
const errors_1 = require("../errors");
/**
 * @class SendGridProvider
 * @description The SendGrid provider for sending emails.
 */
class SendGridProvider {
    constructor(apiKey, weight) {
        this.name = 'sendgrid';
        mail_1.default.setApiKey(apiKey);
        this.weight = weight;
    }
    /**
     * @method send
     * @description Sends an email using SendGrid.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const msg = {
                to: options.to,
                from: options.from,
                subject: options.subject,
                text: options.text,
                html: options.html,
                attachments: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((attachment) => ({
                    content: attachment.content,
                    filename: attachment.filename,
                    type: attachment.contentType,
                    disposition: 'attachment',
                })),
            };
            try {
                const data = yield mail_1.default.send(msg);
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`SendGrid Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.SendGridProvider = SendGridProvider;
