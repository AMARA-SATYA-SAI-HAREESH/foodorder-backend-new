"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MailgunProvider = void 0;
const mailgun_js_1 = __importDefault(require("mailgun.js"));
const form_data_1 = __importDefault(require("form-data"));
const errors_1 = require("../errors");
/**
 * @class MailgunProvider
 * @description The Mailgun provider for sending emails.
 */
class MailgunProvider {
    constructor(apiKey, domain, weight) {
        this.name = 'mailgun';
        this.mailgun = new mailgun_js_1.default(form_data_1.default);
        this.weight = weight;
        this.domain = domain;
        this.apiKey = apiKey;
    }
    /**
     * @method send
     * @description Sends an email using Mailgun.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const mg = this.mailgun.client({
                username: 'api',
                key: this.apiKey,
            });
            const messageData = {
                from: options.from,
                to: options.to,
                subject: options.subject,
                text: options.text,
                html: options.html,
                attachment: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((a) => ({
                    filename: a.filename,
                    data: a.content,
                })),
            };
            try {
                const data = yield mg.messages.create(this.domain, messageData);
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`Mailgun Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.MailgunProvider = MailgunProvider;
