"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MandrillProvider = void 0;
const mailchimp_transactional_1 = __importDefault(require("@mailchimp/mailchimp_transactional"));
const errors_1 = require("../errors");
/**
 * @class MandrillProvider
 * @description The Mandrill provider for sending emails.
 */
class MandrillProvider {
    constructor(apiKey, weight) {
        this.name = 'mandrill';
        this.client = (0, mailchimp_transactional_1.default)(apiKey);
        this.weight = weight;
    }
    /**
     * @method send
     * @description Sends an email using Mandrill.
     * @param {EmailOptions} options - The email options.
     * @returns {Promise<EmailResult>}
     * @throws {EmailProviderError}
     */
    send(options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const message = {
                to: [{ email: options.to }],
                from_email: options.from,
                subject: options.subject,
                text: options.text,
                html: options.html,
                attachments: (_a = options.attachments) === null || _a === void 0 ? void 0 : _a.map((a) => ({
                    name: a.filename,
                    content: a.content,
                    type: a.contentType,
                })),
            };
            try {
                const data = yield this.client.messages.send({ message });
                return {
                    provider: this.name,
                    options,
                    data,
                };
            }
            catch (error) {
                throw new errors_1.EmailProviderError(`Mandrill Error: ${error.message}`, this.name, error);
            }
        });
    }
}
exports.MandrillProvider = MandrillProvider;
